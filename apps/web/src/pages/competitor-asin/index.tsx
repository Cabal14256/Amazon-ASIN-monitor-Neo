import { CatalogPage } from '../catalog';
import { COMPETITOR_CATALOG } from './config';

export default function CompetitorAsinCatalogPage() {
  return <CatalogPage config={COMPETITOR_CATALOG} />;
}
