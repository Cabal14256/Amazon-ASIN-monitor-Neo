# 前端构建环境变量模板
# 复制为根目录 .env 后再按部署环境调整。

# 推荐使用同源相对路径，由 Nginx 将 /api 转发到后端。
# 若填写完整地址，请勿在末尾添加 /，也不要与请求路径重复拼接 /api。
API_BASE_URL=/api

# 数据分析基准脚本可选地址；留空时通过命令行参数传入。
# BENCH_OLD_BASE_URL=http://127.0.0.1:3001
# BENCH_NEW_BASE_URL=http://127.0.0.1:3002
