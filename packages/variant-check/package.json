{
  "name": "@asin-monitor/variant-check",
  "version": "0.0.0",
  "private": true,
  "description": "API/Worker 共享的变体检查业务、状态持久化与结果映射",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "corepack pnpm --filter @asin-monitor/variant-check... build && vitest run",
    "typecheck": "tsc -p tsconfig.test.json --noEmit"
  },
  "dependencies": {
    "@asin-monitor/contracts": "workspace:*",
    "@asin-monitor/db": "workspace:*",
    "@asin-monitor/sp-api": "workspace:*",
    "drizzle-orm": "^0.44.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/pg": "^8.15.4",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
